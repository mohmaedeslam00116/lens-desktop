<task>
Implement the embedding-model system from reference project D:/ai/vane-temp into LENS at D:/ai/New folder (2). Inspect the reference's src/lib/models/base/embedding.ts, provider embedding adapters/registry and their real search/retrieval callers, then adapt the relevant working functionality to this Electron+React TypeScript architecture (frontend/electron/engine and frontend/src). The current engine has no embedding retrieval and filters embedding models from chat discovery. Do not merely expose a setting: wire query/document embeddings and semantic chunk retrieval/ranking into the actual research evidence path so synthesis uses relevant source passages with correct citations. Inspect actual existing behavior before choosing the integration point. Preserve source identity and diversity, bound chunk/batch/context sizes, handle cancellation and timeouts, validate vector count/dimensions/finite values, and make failures visible with an explicit usable fallback to existing research. No unsupported claims of improved quality without measurement.
Provide separate persisted embedding provider/model configuration in Settings, with sensible defaults/migration for existing users, API configuration appropriate to that provider, and model discovery where supported. Adapt reference providers relevant to this app (including remote OpenAI/Gemini and local Ollama); assess the other reference providers and explicitly report any omitted adapter and why. Never use a chat model as an embedding model. Avoid mandatory large model downloads; do not make existing installations unable to research without new credentials. Keep LENS's neutral Vercel/Cursor-like styling, approximate layout, Arabic/English support and dark/light themes. Preserve current storage keys/app IDs/history, report citation identity, chat provider choice, and unrelated UI work. Read BRAND.md/DESIGN.md as needed. No rewrite to Next.js or copying reference app wholesale.
</task>
<verification_loop>
Target frontend has no test runner script. Add focused offline tests using Node's built-in node:test (compiled engine JS may be imported after build) and mocked embedding HTTP responses, testing meaningful behavior: semantic selection, query/document alignment, dimensions/malformed responses, provider failures/fallback, abort, and old settings compatibility as applicable. Never call paid/live model providers or download model weights during tests. In frontend run: npx tsc --noEmit; npm run build:react; npm run build:electron; your focused node --test command. Resolve failures. Do not build an installer. Add a brief setup/verification note explaining when embeddings help and what remains unbenchmarked.
</verification_loop>
<action_safety>
Edit only target application files and necessary tests/docs. Reference D:/ai/vane-temp is READ ONLY, even though it is an extra workspace. Do not read .env/credentials, global settings, or browser storage. No external messaging, deployments, Git initialization/staging/commits, or changes to skills. Target is not a Git repository; orchestrator has a pre-change source snapshot. Use existing packages/native fetch where practical. Do not use any permission-bypass flag or alter permission policy. If tools are denied, stop and report the precise blocker rather than claim implementation. Work directly, no delegation. Keep scope to functional embeddings integration.
</action_safety>
<structured_output_contract>
End with: (1) source system findings and integration decisions, (2) exact files changed, (3) actual gate results/counts, (4) how to configure/use embeddings plus supported/omitted providers and remaining limitations. Distinguish implemented/tested/untested behavior. Keep report under 900 words.
</structured_output_contract>
