interface:
  display_name: "Lens Desktop"
  short_description: "Repo-specific patterns and workflows for lens-desktop"
  default_prompt: "Use the lens-desktop repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true